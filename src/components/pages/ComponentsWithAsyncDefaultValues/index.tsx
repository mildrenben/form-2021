import { ComponentsWithAsyncDefaultValues } from '../../templates/ComponentsWithAsyncDefaultValues'

export const ComponentsWithAsyncDefaultValuesPage = () => {
  // get initial values data

  return (
    <div>
      {' '}
      {
        // replace with some page container
      }
      <ComponentsWithAsyncDefaultValues />
    </div>
  )
}
