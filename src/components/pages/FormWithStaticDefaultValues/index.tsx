import { FormWithStaticDefaultValues } from '../../templates/FormWithStaticDefaultValues'

export const FormWithStaticDefaultValuesPage = () => {
  // get initial values data

  return (
    <div>
      {' '}
      {
        // replace with some page container
      }
      <FormWithStaticDefaultValues />
    </div>
  )
}
