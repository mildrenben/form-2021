import { BlogPostForm } from '../../organisms/forms/BlogPostForm'

export const ComponentsWithAsyncDefaultValues = () => (
  <main>
    <h1>Form with some components having an async default values</h1>
    <BlogPostForm />
  </main>
)
